import styled from "styled-components";

const TimeWrap = styled.div`
  margin-top: 40px;
  font-size: 40px;
  font-family: "LED";
`

const DateContainer = styled.div`
  position: relative;
  h2 {
    display: inline-block;
  }
`

const TimeContainer = styled.div`
  position: relative;

  h2 {
    display: inline-block;
  }
`

export {TimeWrap, DateContainer, TimeContainer}